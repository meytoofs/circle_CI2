{% extends 'base.html.twig' %}

{% block title %}Creer room{% endblock %}

{% block body %}
<div class="wrapper">
    <div class="container">
        <h1>{{ 'form_chat.title'|trans }}</h1>
        {{ include('chat/_form.html.twig') }}
        <a role="button" class="btn btn-outline-danger" href="{{ path('room') }}">{{ 'form_chat.btnBack'|trans }}</a>
    </div>
</div>
{% endblock %}
