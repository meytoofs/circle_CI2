<a class="btn btn-primary" role="button" href="{{ path('idea_proposition_new') }}" style="margin-top: 10px;">{{ 'form_search.btn_create'|trans }}</a>
{{ form_start(form, {attr: {class: 'filter'}}) }}
    {{ form_row(form.q) }}
    
    <button type="submit" class="btn btn-primary w-100">{{ 'form_search.submit'|trans }}</button>
{{ form_end(form) }}