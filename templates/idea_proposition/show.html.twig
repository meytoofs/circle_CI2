{% extends 'base.html.twig' %}

{% block title %}IdeaProposition{% endblock %}

{% block body %}
    <div class="delwar">
        <img width="40rem" src="/uploads/images/featured/{{ idea_proposition.featuredImage }}"</img>
            <img src="{{ idea_proposition.featuredImage }}"></img>
        <div class="title">
            <h2>{{ idea_proposition.title }}</h2>
        </div>
        <div class="test1">
        <div class="content">
            <div class="author">
                <p>Créateur: {{idea_proposition.user }}</p>
            </div>
            <div class="vote-container">
                <div class="face face1" id="onclick">
                    <p>Total score: {{ total_score |number_format(0,',', '')}}</p>
                </div>
                <div class="face face2" id="display">
                    {{ include('idea_proposition/_voteform.html.twig') }}
                </div>
            </div>
            <p id="delwar-content">{{ idea_proposition.content }}</p>
        </div>
        <div class="room_chat">
            <p>En construction</p>
            <p>Vous retrouverez trés bientôt ici même le  chat associé a l'idée, En attandant je vous invite a cliquer sur ce lien pour suivre les derniers salons de discussions tendances !</p>
            <a  action="btn" href="{{ path('room')}}"> Je file discuter !</a>
        </div>
        </div>
    </div>
            {{ idea_proposition.date ? idea_proposition.date|date('Y-m-d H:i:s') : '' }}
            
    <a href="{{ path('idea_proposition_index') }}">back to list</a>
    {% if is_granted (self::EDIT,$idea) %}

        <a href="{{ path('idea_proposition_edit', {'id': idea_proposition.id}) }}">edit</a>
    
    {% endif %}
    {{ include('idea_proposition/_delete_form.html.twig') }}
{% endblock %}
