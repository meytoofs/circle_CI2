{% extends 'base.html.twig' %}

{% block title %}IdeaProposition{% endblock %}

{% block body %}
    <h1>IdeaProposition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ idea_proposition.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ idea_proposition.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ idea_proposition.content }}</td>
            </tr>
            <tr>
                <th>TotalScore</th>
                <td>{{ idea_proposition.totalScore }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ idea_proposition.date ? idea_proposition.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FeaturedImage</th>
                <td>
                    <img width="40rem" src="/uploads/images/featured/{{ idea_proposition.featuredImage }}"</img>
                    <img src="{{ idea_proposition.featuredImage }}"></img>
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('idea_proposition_index') }}">back to list</a>
    {% if is_granted (self::EDIT,$idea) %}

        <a href="{{ path('idea_proposition_edit', {'id': idea_proposition.id}) }}">edit</a>
    
    {% endif %}
    {{ include('idea_proposition/_delete_form.html.twig') }}
{% endblock %}
